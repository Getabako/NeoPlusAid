<div style="max-width:900px;margin:120px auto;padding:0 28px;color:var(--ink)">
  <h1 style="font-size:clamp(28px,4vw,52px)">{{ page.title }}</h1>
  <div style="line-height:1.8;margin-top:24px">{{ page.content }}</div>
</div>
