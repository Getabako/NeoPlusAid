<div style="max-width:1200px;margin:120px auto;padding:0 28px;color:var(--ink)">
  <h1>COLLECTIONS</h1>
  <ul style="list-style:none;padding:0">{% for c in collections %}<li><a href="{{ c.url }}" style="color:var(--ink)">{{ c.title }} ({{ c.products_count }})</a></li>{% endfor %}</ul>
</div>
